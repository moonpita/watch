export enum AppRoute {
    Login = '/login',
    Detail = '/detail',
    Root = '/',
    Favorites = '/favorites',
}

export enum AuthorizationStatus {
    Auth = 'AUTH',
    NoAuth = 'NO_AUTH',
    Unknown = 'UNKNOWN',
}

